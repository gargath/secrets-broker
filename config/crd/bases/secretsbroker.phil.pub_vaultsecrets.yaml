
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.5
  creationTimestamp: null
  name: vaultsecrets.secretsbroker.phil.pub
spec:
  group: secretsbroker.phil.pub
  names:
    kind: VaultSecret
    listKind: VaultSecretList
    plural: vaultsecrets
    singular: vaultsecret
  scope: Namespaced
  validation:
    openAPIV3Schema:
      description: VaultSecret is the Schema for the vaultsecrets API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: VaultSecretSpec defines the desired state of VaultSecret
          properties:
            secretPath:
              description: SecretPath is the path within the specified Vault where
                the secrest value is held
              type: string
            vaultURL:
              description: VaultURL is the URL of the vault server that holds this
                secret. The controller must hold configuration on how to authenticate
                to this vault in order to retrieve the secret
              type: string
          required:
          - secretPath
          - vaultURL
          type: object
        status:
          description: VaultSecretStatus defines the observed state of VaultSecret
          properties:
            state:
              type: string
          required:
          - state
          type: object
      type: object
  version: v1alpha1
  versions:
  - name: v1alpha1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
